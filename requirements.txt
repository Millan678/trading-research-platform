# Trading Research Platform — Requirements
# Core platform is stdlib-only. These are optional/dev dependencies.

# Linting (optional)
ruff>=0.1.0
black>=23.0.0
isort>=5.12.0

# Testing (optional)
pytest>=7.0.0

# Optional: PostgreSQL backend (graceful fallback to SQLite if missing)
# psycopg2-binary>=2.9.0
